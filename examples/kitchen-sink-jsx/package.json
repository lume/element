{
	"type": "module",
	"scripts": {
		"build": "npm run clean && babel src -d dist",
		"dev": "npm run clean && babel src -d dist --watch",
		"clean": "rimraf dist/",
		"serve": "five-server ."
	},
	"dependencies": {
		"@lume/element": "../.."
	},
	"devDependencies": {
		"@babel/cli": "7.21.0",
		"@babel/core": "7.22.0",
		"@babel/plugin-proposal-decorators": "7.22.0",
		"babel-preset-solid": "^1.8.22",
		"five-server": "0.3.1",
		"rimraf": "4.4.0"
	}
}
